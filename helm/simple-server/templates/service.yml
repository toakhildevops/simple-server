apiVersion: v1
kind: Service
metadata:
  name: simple-server
  labels:
    app: simple-server
spec: 
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: simple-server